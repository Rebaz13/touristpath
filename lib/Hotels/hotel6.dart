import 'package:flutter/material.dart';

class Hotel6 extends StatelessWidget {
  const Hotel6({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.amber,
    );
  }
}
